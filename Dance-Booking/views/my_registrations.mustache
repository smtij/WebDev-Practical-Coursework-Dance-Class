<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <!-- Bootstrap CSS CDN -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    /* Optional custom style for dashed border around registration items */
    .registration-item {
      border: 1px dashed #666;
      padding: 10px;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <h1 class="mb-4">{{title}}</h1>
    
    {{#courses}}
      <div class="card mb-4">
        <div class="card-body">
          <h2 class="card-title">{{name}}</h2>
          <p class="card-text">
            <strong>Duration:</strong> {{duration}}<br>
            {{description}}
          </p>
          <h4>Your Registration(s):</h4>
          <!-- Loop over user's registrations for this course -->
          {{#myRegistrations}}
            <div class="registration-item">
              <p class="mb-1"><strong>Name:</strong> {{name}}</p>
              <p class="mb-1"><strong>Email:</strong> {{email}}</p>
              <div>
                <a href="/user/edit-registration/{{courseId}}/{{regIndex}}" class="btn btn-outline-primary btn-sm">Edit Registration</a>
                <form action="/user/unregister/{{courseId}}/{{regIndex}}" method="POST" class="d-inline" onsubmit="return confirm('Are you sure you want to unregister from this course?');">
                  <button type="submit" class="btn btn-outline-danger btn-sm">Unregister</button>
                </form>
              </div>
            </div>
          {{/myRegistrations}}
        </div>
      </div>
    {{/courses}}
    
    {{^courses}}
      <p>You have not registered for any courses yet.</p>
    {{/courses}}
    
    <div class="text-center mt-4">
      <a href="/user/dashboard" class="btn btn-secondary">Back to Dashboard</a>
    </div>
  </div>
  
  <!-- Optional: Bootstrap JS and dependencies -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
