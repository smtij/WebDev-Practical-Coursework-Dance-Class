<!DOCTYPE html> 
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <!-- Bootstrap CSS CDN -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-5">
    <h1 class="mb-4 text-center">{{title}}</h1>
    
    {{#course.registrations}}
      <div class="card mb-3">
        <div class="card-body">
          <p class="card-text"><strong>Name:</strong> {{name}}</p>
          <p class="card-text"><strong>Email:</strong> {{email}}</p>
          <div class="d-flex justify-content-between">
            <a href="/admin/edit-registration/{{course._id}}/{{regIndex}}" class="btn btn-outline-primary">Edit</a>
            <form action="/admin/delete-registration/{{course._id}}/{{regIndex}}" method="POST" onsubmit="return confirm('Are you sure you want to delete this registration?');" class="mb-0">
              <button type="submit" class="btn btn-danger">Delete</button>
            </form>
          </div>
        </div>
      </div>
    {{/course.registrations}}

    {{^course.registrations}}
      <div class="alert alert-info" role="alert">
        No registrations yet.
      </div>
    {{/course.registrations}}

    <div class="text-center mt-4">
      <a href="/admin/manage-courses" class="btn btn-secondary">Back to Manage Courses</a>
    </div>
  </div>
  
  <!-- Optional: Bootstrap JS and dependencies -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
